workflows:
  react-native-ios:
    name: React Native iOS App Store
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: com.bag.remotex

    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.coralspiritual.bibleapp
      vars:
        BUNDLE_ID: "com.coralspiritual.bibleapp"
        TEAM_ID: "99QM459FC5"
        PROFILE_NAME: "Bible profile"
        APP_STORE_APPLE_ID: 0000000000
        SENTRY_DISABLE_AUTO_UPLOAD: "true"
      node: 18
      xcode: 16.4
      cocoapods: default

    scripts:
      - name: Install npm dependencies
        script: |
          cd $CM_BUILD_DIR
          npm install

      - name: Clean cache
        script: |
          cd $CM_BUILD_DIR
          rm -rf node_modules/.cache
          rm -rf $CM_BUILD_DIR/ios/Pods
          rm -rf $CM_BUILD_DIR/ios/Podfile.lock
          rm -rf $CM_BUILD_DIR/ios/build
          pod cache clean --all

      - name: Setup Podfile
        script: |
          cd $CM_BUILD_DIR/ios
          
          # Создаем Podfile с нуля, если его нет
          if [ ! -f Podfile ]; then
            echo "Creating Podfile from scratch"
            cat > Podfile << 'EOF'
          require File.join(File.dirname(`node --print "require.resolve('expo/package.json')"`), "scripts/autolinking")
          require File.join(File.dirname(`node --print "require.resolve('react-native/package.json')"`), "scripts/react_native_pods")
          require File.join(File.dirname(`node --print "require.resolve('@react-native-community/cli-platform-ios/package.json')"`), "native_modules")
          
          platform :ios, '14.0'
          
          prepare_react_native_project!
          
          target 'bible' do
            config = use_native_modules!
            use_frameworks! :static
            flags = get_default_flags()
            
            use_react_native!(
              :path => config[:reactNativePath],
              :hermes_enabled => flags[:hermes_enabled] || false,
              :fabric_enabled => flags[:fabric_enabled] || false
            )
            
            use_expo_modules!
            
            post_integrate do |installer|
              begin
                expo_patch_react_imports!(installer)
              rescue => e
                Pod::UI.warn e
              end
            end
          end
          
          post_install do |installer|
            installer.pods_project.targets.each do |target|
              target.build_configurations.each do |config|
                config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '14.0'
                config.build_settings['SWIFT_VERSION'] = '5.0'
              end
            end
          end
          EOF
            fi
          
          cat Podfile

      - name: Prebuild and install
        script: |
          cd $CM_BUILD_DIR
          npx expo prebuild --platform ios --clean
          cd ios
          pod install --repo-update --verbose

      - name: Set up node for Xcode
        script: |
          cd $CM_BUILD_DIR
          echo "export NODE_BINARY=$(which node)" > ios/.xcode.env.local

      - name: Setup signing
        script: |
          xcode-project use-profiles --verbose

      - name: Build archive with error detection
        script: |
          cd $CM_BUILD_DIR
          set -o pipefail
          
          xcodebuild -workspace "ios/bible.xcworkspace" \
            -scheme "bible" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$CM_BUILD_DIR/build/ios/xcarchive/bible.xcarchive" \
            archive \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM="$TEAM_ID" \
            PROVISIONING_PROFILE_SPECIFIER="$PROFILE_NAME" \
            "CODE_SIGN_IDENTITY=Apple Distribution" \
            IPHONEOS_DEPLOYMENT_TARGET=14.0 \
            ONLY_ACTIVE_ARCH=NO \
            ENABLE_PREVIEWS=NO \
            2>&1 | tee build.log
          
          # Проверяем наличие критических ошибок
          if grep -E "error:|fatal error:" build.log; then
            echo "❌ Build failed with errors:"
            grep -E "error:|fatal error:" build.log
            exit 1
          fi

    artifacts:
      - build/ios/ipa/*.ipa
      - build.log
      - xcodebuild.log