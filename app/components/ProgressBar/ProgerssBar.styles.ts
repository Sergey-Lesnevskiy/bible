import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  containerProgress: {
    width: '100%',
    height: 6,
    flexDirection: 'row',
    borderRadius: 11,
    overflow: 'hidden',
  },
});
