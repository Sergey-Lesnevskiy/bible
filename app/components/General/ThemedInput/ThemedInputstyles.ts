import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  base: {
    minHeight: 132,
    width: '100%',
    borderRadius: 16,
    borderWidth: 0,
    borderColor: 'transparent',
  },
});
